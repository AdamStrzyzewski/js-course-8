// for break nested outer
let breakFlag = false
for (let i = 1 ; i <= 5 ; i++) {
    if(breakFlag){
        break;
    }
    for (let j = 1 ; j <= 2 ; j++){
        if(i >= 3){
            breakFlag = true
            break;
        }
        console.log(`${i}-${j}`)
    }
    console.log(i)
}

